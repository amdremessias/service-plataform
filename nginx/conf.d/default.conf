server {
    listen 80;
    listen [::]:80;
    server_name localhost;

    # Security Headers
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    add_header X-XSS-Protection "1; mode=block";
    add_header Referrer-Policy "no-referrer-when-downgrade";
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

    location / {
        proxy_pass http://web:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /api/ {
        proxy_pass http://api:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Configuration for Certbot (uncomment and adjust when deploying to public IP)
    # location /.well-known/acme-challenge/ {
    #     root /var/www/certbot;
    # }

    # Redirect HTTP to HTTPS (uncomment and adjust when deploying to public IP)
    # server {
    #     listen 80;
    #     listen [::]:80;
    #     server_name seuservico.com.br www.seuservico.com.br;
    #     return 301 https://$host$request_uri;
    # }

    # HTTPS server block (uncomment and adjust when deploying to public IP)
    # server {
    #     listen 443 ssl http2;
    #     listen [::]:443 ssl http2;
    #     server_name seuservico.com.br www.seuservico.com.br;

    #     ssl_certificate /etc/letsencrypt/live/seuservico.com.br/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/seuservico.com.br/privkey.pem;
    #     ssl_trusted_certificate /etc/letsencrypt/live/seuservico.com.br/chain.pem;

    #     # Security Headers (repeated for HTTPS)
    #     add_header X-Frame-Options "SAMEORIGIN";
    #     add_header X-Content-Type-Options "nosniff";
    #     add_header X-XSS-Protection "1; mode=block";
    #     add_header Referrer-Policy "no-referrer-when-downgrade";
    #     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

    #     location / {
    #         proxy_pass http://web:3000;
    #         proxy_http_version 1.1;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection "upgrade";
    #         proxy_set_header Host $host;
    #         proxy_cache_bypass $http_upgrade;
    #     }

    #     location /api/ {
    #         proxy_pass http://api:3001;
    #         proxy_http_version 1.1;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection "upgrade";
    #         proxy_set_header Host $host;
    #         proxy_cache_bypass $http_upgrade;
    #     }
    # }
